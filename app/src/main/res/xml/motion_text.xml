<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Transition
        android:id="@+id/transition_text_mars"
        app:constraintSetStart="@id/start_text"
        app:constraintSetEnd="@id/end_text"
        app:duration="6000"
        app:motionInterpolator="cubic(0,2,0,2,0,8,0,8)">
        <OnClick app:targetId="@+id/tabLayout_mars" app:clickAction="transitionToEnd"/>
    </Transition>

    <ConstraintSet android:id="@+id/start_text">

        <Constraint
            android:id="@+id/text_image_mars"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@id/image_mars_m" />

    </ConstraintSet>

    <ConstraintSet android:id="@+id/end_text">

        <Constraint
            android:id="@+id/text_image_mars"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/image_mars_m" />

    </ConstraintSet>

</MotionScene>
